# kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
networking:
  disableDefaultCNI: true
  kubeProxyMode: "none" # 👈 disables kube-proxy completely
  podSubnet: "10.244.0.0/16" # Cilium default subnet
nodes:
  - role: control-plane
  - role: worker
  - role: worker
